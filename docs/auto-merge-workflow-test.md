# Auto-merge workflow test

This pull request exists to trigger the Exercism auto-merge workflow.

It is intentionally not authored by `exercism-solutions-syncer[bot]`, so the
workflow should skip the auto-merge job.
